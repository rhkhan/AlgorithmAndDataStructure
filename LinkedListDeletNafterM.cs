void DeleteNnodesAfterMnodes(){
  // initialize a counterM for M nodes
  // Traverse upto M nodes
  // store the node after M nodes into a variable
  // set the nodes next to null after traversing the M node
  // initialize another counterN for N nodes
  // traverse the node stored earlier upto N nodes
  // finally set the previously traversed M nodes next to the N nodes next pointer
 }
